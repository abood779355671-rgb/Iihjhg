version: "3.9"

services:
  bot:
    build: .
    container_name: github-zip-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./logs:/app/logs
